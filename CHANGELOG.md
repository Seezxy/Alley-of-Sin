# 更新日志

> 罪恶巷口 (Alley of Sin) — 街头格斗锦标赛卡牌游戏  
> v0.43 之前为平铺式单文件结构，未纳入版本管理。v0.43 重构为 `game/` 包结构并首次提交到 GitHub。

---

## v0.44 (2026-06-07)

### 项目整理

- **文档补全**：迁移并格式化全部历史更新日志（v0.08 ~ v0.43）到 `CHANGELOG.md`
- **提交指南**：新增 `COMMIT_GUIDE.md`（本地参考，已加入 `.gitignore`）
- **README 补全**：补充完整技术架构、依赖表和打包说明

### 技术改进

- **`python -m game` 支持**：新增 `__main__.py`，支持模块方式启动
- **包初始化**：新增 `__init__.py`，声明版本号 `__version__`
- **`.gitignore` 完善**：新增 `COMMIT_GUIDE.md`、`*.dat` 等忽略规则
- **Git 配置**：绑定 GitHub 远程仓库 `Seezxy/Alley-of-Sin.git`

### 结构变迁

```text
v0.42 及以前                     v0.43+（当前）
──────────────────────────────────────────────
平铺式单文件                       game/ 包结构
├── main.py                       ├── main.py
├── card_battle.py                ├── game/
├── tournament.py                 │   ├── app.py
├── constants.py                  │   ├── models.py
├── ...（几十个 .py 混在一起）      │   ├── card_battle.py
└── data/ / images/               │   └── ...（27 个模块）
                                  ├── data/
                                  └── images/
```

---

## v0.43 (2026-06-05)

### 存档加密（防篡改）

- 新增 `save_crypto.py`：使用 Fernet（AES-128-GCM + HMAC）加密存档
- 密钥派生自机器 MAC 地址，阻止跨机器复制存档
- 存档格式从 `data/users.json`（明文）迁移到 `data/users.dat`（密文）
- 首次启动自动迁移旧明文存档并删除原文件
- 文件被篡改后解密失败，自动清空防止崩溃

### 游戏内开发者面板

- 新增 `dev_panel.py`：Toplevel 弹出面板，可视化操作
- 检测 `data/.dev_mode` 时，主菜单和角色选择界面显示"🔧 开发者"按钮
- 支持：货币直设、7 项战斗属性编辑（百分比输入，保存后下次战斗生效）
- `UserManager` 新增 `set_currency()` 方法

### 命令行调试工具

- 新增 `dev_data_tool.py`：支持 `view` / `set-currency` / `add-item` / `unlock-all` / `reset` / `export` / `import` 子命令

### 抽奖概率重平衡

- 缩小低品质三级差距：普通 80→60%，精品 15→24%，优秀 4→11%
- 同步上调稀有及以上品质：罕见 0.5→3%，史诗 0.4→1.5%，传说 0.08→0.4%，神话 0.02→0.1%
- 概率面板改为两列中文展示（左 4 右 3）

### 存档路径固定

- `user_manager.py` 改用 `get_user_data_path()` 获取路径
- 存档保存在 `%APPDATA%/罪恶巷口/users.dat`，程序移动后不丢失
- 首次启动自动从项目目录迁移旧加密/明文文件到 AppData
- `.dev_mode` 文件同步支持 AppData 目录检测

### 项目重构

- 全部代码从平铺式单文件重组为 `game/` 包结构（27 个模块）
- 首次提交到 GitHub（`Seezxy/Alley-of-Sin`）
- 新增 `main.py` 作为程序入口，`game/app.py` 作为主控类
- 新增 `resource_utils.py` 统一处理开发环境与 PyInstaller 打包路径差异

---

## v0.42

### 存档写盘批量化（解决抽奖卡顿核心问题）

- `UserManager` 所有写操作方法新增 `save` 参数，默认 `True` 保持兼容
- `InventoryManager` 对应方法透传 `save` 参数
- `GachaManager` 改为：扣货币（不保存）→ 循环加物品（不保存）→ 最后一次性保存
- 百连抽从 101 次写盘降到 1 次，尽数抽从 N 次降到 1 次
- `SmeltingManager.smelt_items` 同样改为批量写盘

### 界面打开性能优化

- 首次打开子页面跳过 `refresh_all()`，避免双重构建
- `GachaFrame.refresh_all()` 改为增量刷新（仅更新货币和按钮状态），不再全量销毁重建
- 装备图鉴标签页懒加载：只在用户点击"装备图鉴"标签时才创建 `CatalogueUI`
- 效果：首次打开抽奖 3.75s→0.91s，后续打开 1.21s→即时（0.0004s）

### 背包分页系统

- 新增分页功能：每页 50 件物品
- 添加页码导航栏：上一页 / 下一页 / 跳转输入框 / 页码显示
- 筛选和搜索时自动回到第一页
- 修复物品创建时闪烁问题：`<Configure>` 事件绑定推迟到所有物品 grid 完成后

### 熔炼性能优化

- `SmeltingManager` 新增 `_compute_inventory_counts()` 单次遍历预计算，带缓存
- `get_smelting_options()` 使用缓存计数
- 效果：2016 件物品时 `get_smelting_options` 从完全卡死到 0.003s

### 批量熔炼功能

- 选中熔炼选项后底部出现次数控制：`[−] [N] [+] [最大] [熔炼 ×N]`
- 支持一次性熔炼多次
- 新增 `smelt_multiple()` 方法
- 信息面板显示最大可熔炼次数

### 大量抽奖结果显示优化（解决尽数抽闪退）

- 超过 30 件物品时显示统计摘要（稀有度分布 + 类型分布 + 百分比），不再逐件创建 widget
- 30 件及以下保持逐件显示
- 修复逐件显示时 Configure 事件导致的位置闪烁

---

## v0.41

- 锦标赛界面"返回"按钮点击后弹出确认对话框
- 确认提示："确定要返回吗？当前锦标赛进度将不会保存。"
- 防止误操作导致比赛中途退出

---

## v0.40

### 系统优化和 Bug 修复

- **背包容量**：上限从 1000 调整为 5000，修复容量显示不实时更新
- **装备图鉴排序**：参照背包排序（按稀有度），相同稀有度按拼音排序
- **滚轮速度优化**：装备图鉴滚轮移动距离 3x（3→6 单位），保留 50ms 防重复机制

### 属性负数处理系统

- 创建统一的属性工具类（`attribute_utils.py`）：
  - `clamp_attribute()` — 禁止负数属性，自动修正为 0
  - `format_attribute()` — 正确格式化属性显示（不显示负号）
  - `safe_add_attributes()` — 安全累加属性，应用边界检查
  - `validate_and_fix_attributes()` — 验证并修复属性字典
- 修改所有属性相关文件使用统一工具类
- 属性边界规则：百分比属性上限 100%，免伤上限 99%

---

## v0.39

### 游戏打包系统

- 重写打包脚本，使用 PyInstaller 创建独立 exe 文件
- 新增 `resource_utils.py`：处理打包后资源文件访问，支持从 exe 内部读取资源
- 修改用户数据管理系统支持用户目录存储
- 用户数据保存在系统用户目录（`%APPDATA%`），与 exe 分离
- 提供多种打包选项：单文件版本 / 文件夹版本 / 测试环境配置

---

## v0.38

### 无尽模式 AI 强化机制重新设计

- 新机制：赢 n 场就在初始属性上抽 3n 项加强（而非每轮固定 3 项）
- 只限制免伤属性上限为 0.99，其他属性不设上限
- 免伤达到 10 次后从强化选项中排除（重抽机制）
- 强化数值：吸血 +0.1、免伤 +0.1、伤害加成 +0.1、暴击率 +0.10、暴击伤害 +1.5、闪避 +0.05、最大生命值 +1

---

## v0.37

### 新增无尽模式

- 在难度选择界面添加"无尽"选项（第五个难度）
- 地狱难度起步（ai_rewards=3），非淘汰赛制，只要不输就能一直战斗
- 奖金翻倍系统：第 n 场奖金 = 3 × 2^(n-1)，指数级增长
- 可随时结束战斗并结算累计奖金
- 新增 `endless_mode.py`：管理连胜、奖金、AI 强化状态
- 战斗界面左上角实时显示当前场次和奖金信息

---

## v0.36

### 抽奖概率和装备系统全面调整

- 新增稀有度"精品"（白色），位于普通和优秀之间
- 调整概率：普通 80%、精品 15%、优秀 4%、罕见 0.5%、史诗 0.4%、传说 0.08%、神话 0.02%
- 下移现有装备稀有度一档
- 装备属性规则重新设计（按稀有度分档：正负属性数、HP 倍率、概率加成）
- 完整重新生成装备系统：总计 381 件装备（普通 192 件 → 神话 3 件，每档翻倍递减）
- 用户数据完全清空重置，新默认用户 1000 钞票起步

---

## v0.35

### Toast 提示系统和界面优化

- 新增 `toast.py`：淡出提示框功能，支持 4 种类型（info/success/warning/error）
- 替换所有 `messagebox.showinfo/showwarning/showerror` 为 Toast 提示
- 保留必要的确认对话框（逃离战斗、丢弃物品、创建用户、熔炼确认）
- 游戏规则和牌库查看从 Toplevel 弹窗改为战斗界面右侧信息面板显示
- 新增百连抽功能（6000 钞票，一次抽取 100 个物品）
- 改进尽数抽显示：按钮文本显示具体抽数和总价格

---

## v0.34

### 新增装备熔炼系统

- 100 件低级装备合成 1 件高一级装备（普通→优秀→罕见→史诗→传说→神话）
- 随机类别八折优惠：80 件装备
- 新增 `smelting_manager.py`（熔炼逻辑）和 `smelting_frame.py`（熔炼界面）
- 智能筛选：自动过滤已装备和不符合条件的物品
- 确认对话框 + 结果反馈 + 错误处理

---

## v0.33

### 用户数据存储优化

- 背包物品只存储最小化信息（instance_id、item_id、equipped、created_at）
- 物品详细信息动态从 `items.json` 加载，大幅减少存档文件大小

### 背包分类和排序

- 新增分类筛选：全部 / 武器 / 护甲 / 饰品
- 新增排序功能：按名称 / 稀有度（神话→普通）

### 滚轮绑定全面修复

- 创建统一滚轮绑定工具模块（`wheel_utils.py`）
- 修复所有界面滚轮支持，解决光标不能在所有位置滚动的问题
- 使用时间阈值（50ms）防止滚轮事件被多次触发

---

## v0.32

### 抽奖"全部装备"混抽

- 修复"全部装备"按钮点击无响应问题
- 现在可以从所有类型装备中随机混抽

### 装备图鉴全面优化

- 创建新的图鉴管理器模块（`catalogue_manager.py`），分离数据管理和 UI
- 使用数据类 `CatalogueItem` 封装物品数据
- 添加搜索框、筛选按钮、排序按钮
- 实时统计显示

### 背包界面布局优化

- 物品网格从 3 列改为 5 列
- 每个物品宽度从 280px 减少到 160px

---

## v0.31

### 界面集成优化

- 冠军获胜从 messagebox 改为集成在当前界面显示庆祝界面
- 游戏结束从 messagebox 改为集成显示
- 所有界面添加暗紫色边框保持视觉一致性

### 属性系统修复

- 修复玩家属性传递问题
- 修复 HP 重置逻辑
- 修复装备属性重复应用问题（保存的属性不再包含装备加成）
- 修复角色选择和锦标赛界面装备属性显示

---

## v0.30

### 角色选择界面全面优化

- 主窗口高度从 600px 增加到 720px（+20%）
- 五个角色按钮全部放在一行显示
- 所有元素居中显示
- 详细信息区域添加暗紫色边框
- 修复滚轮事件绑定

---

## v0.29

### 装备图鉴增强

- 可以查询所有装备，不仅仅是当前抽奖池
- 新增"全部装备"抽奖池选项（仅用于图鉴查看，不能抽奖）
- 按类型分组显示所有装备
- 显示每个装备的详细信息

---

## v0.28

- 从所有装备中删除 `attack_speed`（攻击速度）属性
- 从现有用户数据中删除 `attack_speed` 属性

---

## v0.27

### UI 改进和滚轮支持

- 角色选择界面显示装备加成后的属性（区分基础和最终属性）
- 背包物品信息栏高度从 150px 增加到 200px
- 背包物品网格从 4 列改为 3 列
- 为所有滚动区域添加鼠标滚轮支持（Windows/Mac/Linux）

---

## v0.26

### 装备属性应用

- 装备属性正确应用到角色战斗属性
- 修改 `CardBattle._init_players()` 从装备获取属性并应用到玩家
- 属性面板显示装备加成后的最终属性

---

## v0.25

### 抽奖界面和菜单刷新修复

- 修复抽奖界面货币显示不实时更新
- 修复尽数抽按钮状态不更新
- 修复无法切换抽奖卡池
- 修复菜单货币显示不更新（返回时调用 `refresh_user_info()`）

---

## v0.24

- 修复抽奖系统物品添加逻辑：`create_item_instance` → `add_item_to_inventory`
- 物品现在正确保存到用户文件，货币扣除正常

---

## v0.23

### 新增完整抽奖系统

- 获胜奖励减半：简单 30、标准 60、困难 120、地狱 240
- 背包容量上限增加到 1000 格
- 主菜单开始按钮下方添加背包和抽奖按钮
- 三个抽奖池：武器池、护甲池、饰品池
- 单抽 60 钞票、十连抽 600 钞票、尽数抽
- 概率：普通 80%、稀有 15%、史诗 3.4%、传说 1.5%、神话 0.1%
- 新增 37 件装备物品
- 新增 `gacha_manager.py`、`gacha_frame.py`

---

## v0.22

### 新增货币系统

- 货币为"钞票"（💵）
- 锦标赛冠军奖励：简单 60、标准 120、困难 240、地狱 480
- 用户数据中添加 `currency` 字段
- 主菜单和背包界面显示货币数量

### 新增测试角色"程序员"

- 伤害加成 300%（3.0），免伤 95%（0.95）
- 用于测试胜利流程

### 修复货币显示刷新

- 菜单界面货币不实时更新
- 在 `Menu` 类中添加 `refresh_user_info()` 方法

---

## v0.21

- 背包按钮从主菜单移到选择游戏模式/难度/角色界面左下角
- 背包界面从独立窗口（Toplevel）改为在主窗口中切换显示（Frame）
- 新增 `inventory_frame.py`

---

## v0.20

### 新增背包系统

- 物品定义系统（`data/items.json`）
- 背包管理：存储、装备、卸下、丢弃
- 装备系统：武器、护甲、饰品三种槽位
- 图形化界面（背包按钮 → 背包管理界面）
- 属性加成：装备物品提供战斗属性加成
- 新增 `item_manager.py`、`inventory.py`、`item_ui.py`

---

## v0.19

### 新增用户登录系统

- 游戏启动时显示登录界面
- 支持新用户创建和现有用户登录（1-50 字符昵称）
- 新增 `user_manager.py`，使用 JSON 文件存储用户数据
- 菜单界面右上角显示当前用户昵称

### 对战界面增强

- 顶部信息栏显示当前对战难度
- 左侧属性面板显示电脑强化次数
- 修复强化次数计算逻辑（根据属性值与基础值的差异估算）

---

## v0.18

- 修复背景图片显示问题：统一所有界面使用 `bg_menu.jpeg`
- 修复图片加载时机问题确保背景立即显示
- 修改 `fill_background_with_image` 函数支持延迟加载和自动缩放
- 删除所有用不到的图片文件，简化背景填充系统

---

## v0.17

- 新增角色"小偷"

---

## v0.16

### 接入图片资源

- 主菜单背景图（Canvas 铺底，随窗口缩放）
- 角色选择界面显示角色头像
- 战斗界面手牌和出牌槽显示牌面图片
- 槽位改用固定尺寸 Frame 包裹，空位与有牌时宽度一致不再跳动
- 修复结算动画后槽位颜色恢复逻辑

---

## v0.15

### 新增角色"泰拳手"

- 牌库含 10 张肘牌（替换 10 张拳），肘牌序号为偶数
- 肘牌：额外暴击率 +50%，暴击额外伤害 +100%
- 新增"查看牌库"按钮，弹窗展示双方牌库、弃牌堆、手牌、场上牌及重洗次数

---

## v0.14

### 新增难度选择界面

- 位于模式选择与角色选择之间
- 简单 / 标准 / 困难 / 地狱，对应 AI 每轮获得 0/1/2/3 个随机奖励
- 重写游戏内规则弹窗，分"快速上手"和"详细规则"两段

---

## v0.13

### 结算动画

- 每个位置依次高亮（金色），逐步显示结算过程并实时更新血量
- 有人死亡立即终止后续位置结算
- 重构结算逻辑为步骤列表（steps），支持动态追加反击日志
- 暴击提示调整到伤害结果之前显示

### 偷牌机制完善

- 偷来的牌为复制牌（名字加 `'`），原牌进弃牌堆
- 复制牌打出后不进弃牌堆，复制牌也可被再次偷走
- 修复重洗牌时弃牌堆逻辑
- 修复结算动画颜色恢复问题

---

## v0.12

### 新增角色"打手"

- 伤害加成 5%，免伤 5%

### UI 重做

- 左侧属性面板同时显示双方属性
- 重做锦标赛赛前界面：阶段进度条、角色属性卡片展示全部 7 项属性
- "重置游戏"按钮改为"逃离战斗"

### AI 属性系统

- `CardBattle` 支持 `ai_attrs` 参数
- AI 每场胜利后随机获得一个奖励，累积到下一场

### 战斗结算重写

- 双向同位置结算：拳对拳数字大的先以 1.5 倍出手，对方未死则以 1.0 倍反击
- 平局双方同时 1.0 倍
- 修复原双次单向调用导致的槽位数据污染

---

## v0.11

### Bug 修复

- 修复 `menu.py` 无限递归
- 修复 `CardBattle` 缺少 `load_player_attrs` 方法
- 修复 `game_over` 时 `hand_buttons` 未初始化崩溃
- 修复 `tournament` 重复 `reset` 导致跨局属性继承失效
- 修复 HP+奖励跨局不生效

### UI 重构

- 三列布局（属性/战斗/结算），比例 1:5:1，使用 `place` 百分比精确控制
- 战斗日志与出牌区比例 1:5，手牌区贴底显示
- 结算信息改为只显示玩家视角

### 数值调整

- 暴击率奖励 +10%、暴击伤害奖励 +150%、血量奖励 +1
- 每回合发牌数改为 4 张
- 最小手牌上限保证为 1

---

## v0.10

- 实现角色选择功能（弹窗形式）

---

## v0.09

- 更新规则说明
- 新增点击取回手牌功能

---

## v0.08

- 🎉 项目启动
- 搭建基础框架，完成对战核心需求
- 实现吸血、暴击、伤害增幅等特效
- 待解决：AI 属性、角色选取、AI 进化、货币系统等
